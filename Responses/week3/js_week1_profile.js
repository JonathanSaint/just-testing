// Stored user's info using Variables of different data types.
let studentStatus = true; //Boolean datatypes
const username = "Arinda Jonathan"; //Sting data type
let age = 17; //Number datatypes


if (studentStatus = true && age <= 18 >= 13) {
        console.log(username,"is",age,"years old","and is in High School.")
    } else if (studentStatus = true && age <= 12) {
        console.log(username,"is",age,"years old","and is in Middle School.")
    } else {
        console.log(username,"is",age,"years old","and is not a student.")
    }