const display = document.getElementById("display");
const name = document.getElementById("name")

function showOnDisplay(name) {
    if (name.value === "") {
        console.log("it's a word")
    }
}