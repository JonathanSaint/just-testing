## 📘 Python Assignment — Python Syntax & Statements (Week 1)

**Due:** Next Thursday
**Topic:** Variables, Data Types, Operators, Conditionals, Loops

---

## ⭐ Instructions

Complete all the tasks below in a **single Python file** named:

```
week1_python_syntax_assignment.py
```

For **each task**:

* Write valid Python code
* Use `print()` to display output
* Add **comments** explaining what your code does
* Run the file using Python (`python filename.py`)

---

## 📝 Assignment Tasks

### 1️⃣ Variables & Data Types

Create variables for the following:

* Your name (string)
* Your age (integer)
* Your height in meters (float)
* Whether you are a student (boolean)

Print each variable.

---

### 2️⃣ Arithmetic Operators

Create two number variables and perform:

* Addition
* Subtraction
* Multiplication
* Division
* Modulus

Print each result.

---

### 3️⃣ String Operations

Create two string variables:

* First name
* Last name

Concatenate them into a full name and print it.

---

### 4️⃣ Comparison Operators

Create two numbers and compare them using:

* `==`
* `!=`
* `>`
* `<`
* `>=`
* `<=`

Print the result of each comparison.

---

### 5️⃣ Conditional Statements

Write an `if...elif...else` statement that:

* Checks a student’s score
* Prints:

  * `"Excellent"` if score ≥ 80
  * `"Pass"` if score ≥ 50
  * `"Fail"` otherwise

---

### 6️⃣ Loops

#### a) `for` Loop

Use a `for` loop to print numbers **1 to 10**.

#### b) `while` Loop

Use a `while` loop to print numbers **10 to 1**.

---

### 7️⃣ Lists

Create a list of **5 favorite fruits**.

* Print the entire list
* Print the first and last item

---

### 8️⃣ Dictionaries

Create a dictionary called `person` with:

* `name`
* `age`
* `is_student`

Print each value using the key.

---

### 9️⃣ User Input

Write a program that:

* Asks the user for their name
* Asks the user for their age
* Prints a message like:

```
Hello John, you are 20 years old.
```

---

### 🔟 Bonus Challenge (Optional)

Write a program that:

* Loops through a list of numbers
* Prints only the **odd numbers**

---

## ✅ Before Submitting

Make sure your file:

✔ Runs without errors
✔ Uses correct Python indentation
✔ Contains comments explaining your logic
✔ Uses meaningful variable names
✔ Produces clear output
