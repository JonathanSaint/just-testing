## 📘 CSS Assignment — CSS Syntax & Selectors (Week 1)

**Due:** Next Saturday
**Topic:** CSS Syntax, Selectors, Colors, Fonts, Box Model

---

## ⭐ Instructions

Complete all the tasks below using **two files**:

```
index.html
styles.css
```

For **each task**:

* Write clean and valid CSS
* Use correct selectors and properties
* Add **CSS comments** explaining what each rule does
* Link your CSS file properly to the HTML file

⚠️ **Do NOT use JavaScript or CSS frameworks** (Bootstrap, Tailwind, etc.)

---

## 📝 Assignment Tasks

### 1️⃣ Linking CSS to HTML

In `index.html`:

* Create a basic HTML structure
* Link the CSS file using:

```html
<link rel="stylesheet" href="styles.css">
```

---

### 2️⃣ Element Selector

In `styles.css`:

* Style all `<p>` elements to:

  * Text color: blue
  * Font size: 16px

---

### 3️⃣ Class Selector

Create a class called:

```css
.highlight
```

Apply it to an element in HTML and style it with:

* Yellow background
* Bold text

---

### 4️⃣ ID Selector

Create an ID called:

```css
#main-title
```

Style it with:

* Font size: 32px
* Centered text

Apply it to a heading in HTML.

---

### 5️⃣ Backgrounds & Colors

Style the `<body>` to have:

* Light gray background color
* Dark text color

---

### 6️⃣ Fonts

Apply the following font styles to the page:

* Font family: Arial, sans-serif
* Use `font-weight`, `font-style`, and `text-decoration` at least once

---

### 7️⃣ Box Model

Create a `<div>` with class:

```css
.card
```

Style it with:

* Width: 300px
* Padding: 20px
* Border: 2px solid black
* Margin: 15px

---

### 8️⃣ Hover Effect

Add a hover effect to a button:

* Change background color on hover
* Change text color on hover

Use:

```css
:hover
```

---

### 9️⃣ Layout Basics

Use CSS to:

* Center a `<div>` horizontally
* Add spacing between elements using `margin`

---

### 🔟 Bonus Challenge (Optional)

Create a navigation bar using:

* `<ul>` and `<li>`
* Remove default list styling
* Display items horizontally

---

## ✅ Before Submitting

Make sure:

✔ CSS file is linked correctly
✔ All selectors work as expected
✔ Comments explain each CSS rule
✔ No inline styles are used
✔ Code is clean and readable
