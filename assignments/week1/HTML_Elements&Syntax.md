# 📘 HTML Assignment — Core HTML Elements (Week 1)

**Due:** Next Thursday
**Topic:** HTML Structure, Text, Links, Images, Forms

---

## ⭐ Instructions

Complete all the tasks below in a **single HTML file** named:

```
week3_html_elements_assignment.html
```

For **each task**:

* Use **proper HTML elements**
* Follow correct HTML structure
* Add **HTML comments** explaining what each section does
* Open the file in a browser to verify it works correctly

⚠️ **Do NOT use CSS or JavaScript** for this assignment.

---

## 📝 Assignment Tasks

### 1️⃣ Basic HTML Structure

Create a valid HTML document that includes:

* `<!DOCTYPE html>`
* `<html>`, `<head>`, and `<body>`
* A `<title>` with the text:

```
HTML Elements Assignment
```

---

### 2️⃣ Headings & Paragraphs

Inside the `<body>`:

* Add an `<h1>` heading with your **full name**
* Add an `<h2>` heading with the text:

```
About Me
```

* Add a `<p>` paragraph describing yourself (at least 3 sentences)

---

### 3️⃣ Text Formatting Elements

Create a paragraph that includes:

* **Bold text** using `<strong>`
* *Italic text* using `<em>`
* <u>Underlined text</u> using `<u>`
* A line break using `<br>`

---

### 4️⃣ Links & Images

Add the following:

* A hyperlink using `<a>` that points to:

  ```
  https://www.google.com
  ```

  The link text should be **"Visit Google"**.

* An image using `<img>`:

  * Use any image URL
  * Add a meaningful `alt` attribute
  * Set `width="200"`

---

### 5️⃣ Lists

Create **two lists**:

* An **unordered list** (`<ul>`) of 3 favorite hobbies
* An **ordered list** (`<ol>`) of 3 daily activities

---

### 6️⃣ Table

Create a table using:

* `<table>`, `<tr>`, `<th>`, and `<td>`

The table should have:

| Subject  | Score |
| -------- | ----- |
| Math     | 85    |
| English  | 90    |
| Computer | 95    |

---

### 7️⃣ Form Elements

Create a simple form using `<form>` that includes:

* Text input for **Name**
* Email input for **Email**
* Password input for **Password**
* Radio buttons for **Gender**
* A dropdown (`<select>`) for **Country**
* A submit button

⚠️ The form does **not** need to submit data anywhere.

---

### 8️⃣ Semantic HTML Elements

Use at least **three** of the following elements:

* `<header>`
* `<nav>`
* `<main>`
* `<section>`
* `<article>`
* `<footer>`

Add brief content inside each one.

---

## ✅ Before Submitting

Make sure your file:

✔ Opens correctly in a browser
✔ Uses valid HTML elements
✔ Contains HTML comments explaining each section
✔ Has proper indentation and formatting
✔ Does **not** include CSS or JavaScript

