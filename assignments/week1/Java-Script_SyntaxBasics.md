## 📘 JavaScript Assignment — JavaScript Syntax Basics (Week 1)

**Due:** Next Saturday
**Topic:** Variables, Data Types, Operators, Conditionals, Loops

---

## ⭐ Instructions

Complete all the tasks below in a **single JavaScript file** named:

```
week3_javascript_syntax_assignment.js
```

For **each task**:

* Write valid JavaScript code
* Use `console.log()` to display output
* Add **comments** explaining what each part of the code does
* Run the file in the browser console or Node.js

---

## 📝 Assignment Tasks

### 1️⃣ Variables & Data Types

Create variables using `let` or `const` for the following:

* Your name (string)
* Your age (number)
* Whether you are a student (boolean)

Print each variable to the console.

---

### 2️⃣ Arithmetic Operators

Create two number variables and use them to demonstrate:

* Addition
* Subtraction
* Multiplication
* Division

Log each result to the console.

---

### 3️⃣ String Concatenation

Create two string variables:

* First name
* Last name

Combine them into one full name and print the result.

---

### 4️⃣ Comparison Operators

Create two number variables and compare them using:

* `==`
* `===`
* `!=`
* `>`
* `<`

Print the result of each comparison.

---

### 5️⃣ Conditional Statements

Write an `if...else` statement that:

* Checks if a person is **18 or older**
* Prints `"Adult"` if true
* Prints `"Minor"` if false

---

### 6️⃣ Switch Statement

Create a variable called `day` with a value from 1–7.

Use a `switch` statement to print the corresponding day of the week.

---

### 7️⃣ Loops

#### a) `for` Loop

Print numbers **1 to 10** using a `for` loop.

#### b) `while` Loop

Print numbers **10 to 1** using a `while` loop.

---

### 8️⃣ Arrays

Create an array of **5 favorite foods**.

* Print the entire array
* Print the first and last item

---

### 9️⃣ Objects

Create an object called `person` with the following properties:

* `name`
* `age`
* `isStudent`

Print each property using dot notation.

---

### 🔟 Bonus Challenge (Optional)

Write a program that:

* Loops through an array of numbers
* Prints only the **even numbers**

---

## ✅ Before Submitting

Make sure your file:

✔ Runs without errors
✔ Uses proper JavaScript syntax
✔ Contains comments explaining your logic
✔ Uses `let` and `const` appropriately
✔ Outputs results clearly using `console.log()`

